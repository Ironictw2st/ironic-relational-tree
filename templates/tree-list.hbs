<div class="tree-list">
  <div class="tree-list-header">
    <button class="back-button">
      <i class="fas fa-arrow-left"></i>
      <span>Back</span>
    </button>
    <h2>{{categoryName}}</h2>
    <div class="header-actions">
      {{#if isGM}}
      <button class="import-tree" title="Import from JSON">
        <i class="fas fa-upload"></i>
      </button>
      <button class="export-all" title="Export All">
        <i class="fas fa-file-export"></i>
      </button>
      {{/if}}
      <button class="create-tree">
        <i class="fas fa-plus"></i>
        <span>New Tree</span>
      </button>
    </div>
  </div>

  <div class="search-bar">
    <i class="fas fa-search search-icon"></i>
    <input type="text" class="search-input" placeholder="Search trees..." value="{{searchFilter}}" />
    <button class="search-clear" title="Clear search">
      <i class="fas fa-times"></i>
    </button>
  </div>

  <div class="tree-items">
    {{#each trees}}
    <div class="tree-item" data-tree-id="{{this.id}}">
      <div class="tree-item-info">
        <i class="fas fa-project-diagram tree-item-icon"></i>
        <div class="tree-item-details">
          <span class="tree-name">{{this.name}}</span>
          <span class="tree-meta">{{this.nodeCount}} characters · {{this.connectionCount}} connections</span>
        </div>
      </div>
      <div class="tree-item-actions">
        {{#if ../isGM}}
        <button class="export-tree" title="Export JSON">
          <i class="fas fa-download"></i>
        </button>
        {{/if}}
        <button class="edit-tree" title="Rename">
          <i class="fas fa-edit"></i>
        </button>
        <button class="delete-tree" title="Delete">
          <i class="fas fa-trash"></i>
        </button>
      </div>
    </div>
    {{else}}
    <div class="no-trees">
      <i class="fas fa-folder-open"></i>
      {{#if ../searchFilter}}
      <p>No matching trees found</p>
      <span>Try a different search term</span>
      {{else}}
      <p>No trees created yet</p>
      <span>Click "New Tree" to get started</span>
      {{/if}}
    </div>
    {{/each}}
  </div>
</div>

<style>
.tree-list {
  display: flex;
  flex-direction: column;
  height: 100%;
}

.tree-list-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0.75rem 1rem;
  border-bottom: 1px solid var(--color-border-dark);
  background: var(--color-bg-option);
}

.tree-list-header h2 {
  margin: 0;
  font-size: 1.1em;
}

.header-actions {
  display: flex;
  gap: 0.5rem;
  align-items: center;
}

.tree-list-header button {
  display: flex;
  align-items: center;
  gap: 0.4rem;
  padding: 0.5rem 0.75rem;
  border-radius: 4px;
  border: 1px solid var(--color-border-dark);
  background: var(--color-bg-btn);
  cursor: pointer;
  transition: all 0.2s;
}

.tree-list-header button:hover {
  background: var(--color-bg-btn-hover);
}

.tree-list-header .create-tree {
  background: #000000;
  border-color: #000000;
  color: white;
}

.tree-list-header .create-tree:hover {
  filter: brightness(1.1);
}

.header-actions .import-tree,
.header-actions .export-all {
  width: 36px;
  height: 36px;
  padding: 0;
  justify-content: center;
}

/* Search Bar */
.search-bar {
  display: flex;
  align-items: center;
  padding: 0.5rem 1rem;
  background: var(--color-bg-option);
  border-bottom: 1px solid var(--color-border-dark);
  gap: 0.5rem;
}

.search-icon {
  color: var(--color-text-dark-secondary);
}

.search-input {
  flex: 1;
  padding: 0.5rem;
  border: 1px solid var(--color-border-dark);
  border-radius: 4px;
  background: var(--color-bg-btn);
  font-size: 0.95em;
}

.search-input:focus {
  outline: none;
  border-color: #000000;
}

.search-clear {
  width: 28px;
  height: 28px;
  padding: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  border: none;
  background: transparent;
  color: var(--color-text-dark-secondary);
  cursor: pointer;
  border-radius: 4px;
  transition: all 0.2s;
}

.search-clear:hover {
  background: var(--color-bg-btn-hover);
  color: var(--color-text-dark-primary);
}

/* Tree Items */
.tree-items {
  flex: 1;
  overflow-y: auto;
  padding: 0.75rem;
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.tree-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0.75rem 1rem;
  background: var(--color-bg-btn);
  border: 1px solid var(--color-border-dark);
  border-radius: 6px;
  cursor: pointer;
  transition: all 0.2s;
}

.tree-item:hover {
  background: var(--color-bg-btn-hover);
  border-color: #000000;
}

.tree-item-info {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  min-width: 0;
}

.tree-item-icon {
  font-size: 1.25rem;
  color: #000000;
  flex-shrink: 0;
}

.tree-item-details {
  display: flex;
  flex-direction: column;
  gap: 0.2rem;
  min-width: 0;
}

.tree-name {
  font-weight: bold;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.tree-meta {
  font-size: 0.8em;
  color: var(--color-text-dark-secondary);
}

.tree-item-actions {
  display: flex;
  gap: 0.25rem;
  opacity: 0;
  transition: opacity 0.2s;
}

.tree-item:hover .tree-item-actions {
  opacity: 1;
}

.tree-item-actions button {
  width: 30px;
  height: 30px;
  padding: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  border: 1px solid var(--color-border-dark);
  border-radius: 4px;
  background: var(--color-bg-option);
  cursor: pointer;
  transition: all 0.2s;
}

.tree-item-actions button:hover {
  background: var(--color-bg-option-hover);
}

.tree-item-actions .export-tree:hover {
  background: #000000;
  border-color: #000000;
  color: white;
}

.tree-item-actions .delete-tree:hover {
  background: #ff4444;
  border-color: #ff4444;
  color: white;
}

.no-trees {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 3rem;
  color: var(--color-text-dark-secondary);
  text-align: center;
}

.no-trees i {
  font-size: 3rem;
  margin-bottom: 1rem;
  opacity: 0.5;
}

.no-trees p {
  margin: 0 0 0.5rem 0;
  font-size: 1.1em;
  font-weight: bold;
}

.no-trees span {
  font-size: 0.9em;
}
</style>